import 'package:flutter_test/flutter_test.dart';

void main() {
  group('PatientNotifier', () {
    test('placeholder until patient provider test harness is added', () {
      expect(true, isTrue);
    });
  });
}
